services:
  - type: web
    name: telegram-bot
    env: node
    region: singapore
    buildCommand: npm install
    startCommand: node bot.js
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false